// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

/**
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as meraki from "@pulumi/meraki";
 *
 * const example = new meraki.networks.SwitchDscpToCosMappings("example", {
 *     mappings: [{
 *         cos: 1,
 *         dscp: 1,
 *         title: "Video",
 *     }],
 *     networkId: "string",
 * });
 * export const merakiNetworksSwitchDscpToCosMappingsExample = example;
 * ```
 *
 * ## Import
 *
 * ```sh
 * $ pulumi import meraki:networks/switchDscpToCosMappings:SwitchDscpToCosMappings example "network_id"
 * ```
 */
export class SwitchDscpToCosMappings extends pulumi.CustomResource {
    /**
     * Get an existing SwitchDscpToCosMappings resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: SwitchDscpToCosMappingsState, opts?: pulumi.CustomResourceOptions): SwitchDscpToCosMappings {
        return new SwitchDscpToCosMappings(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'meraki:networks/switchDscpToCosMappings:SwitchDscpToCosMappings';

    /**
     * Returns true if the given object is an instance of SwitchDscpToCosMappings.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is SwitchDscpToCosMappings {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === SwitchDscpToCosMappings.__pulumiType;
    }

    /**
     * An array of DSCP to CoS mappings. An empty array will reset the mappings to default.
     */
    public readonly mappings!: pulumi.Output<outputs.networks.SwitchDscpToCosMappingsMapping[]>;
    /**
     * networkId path parameter. Network ID
     */
    public readonly networkId!: pulumi.Output<string>;

    /**
     * Create a SwitchDscpToCosMappings resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: SwitchDscpToCosMappingsArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: SwitchDscpToCosMappingsArgs | SwitchDscpToCosMappingsState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as SwitchDscpToCosMappingsState | undefined;
            resourceInputs["mappings"] = state ? state.mappings : undefined;
            resourceInputs["networkId"] = state ? state.networkId : undefined;
        } else {
            const args = argsOrState as SwitchDscpToCosMappingsArgs | undefined;
            if ((!args || args.networkId === undefined) && !opts.urn) {
                throw new Error("Missing required property 'networkId'");
            }
            resourceInputs["mappings"] = args ? args.mappings : undefined;
            resourceInputs["networkId"] = args ? args.networkId : undefined;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(SwitchDscpToCosMappings.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering SwitchDscpToCosMappings resources.
 */
export interface SwitchDscpToCosMappingsState {
    /**
     * An array of DSCP to CoS mappings. An empty array will reset the mappings to default.
     */
    mappings?: pulumi.Input<pulumi.Input<inputs.networks.SwitchDscpToCosMappingsMapping>[]>;
    /**
     * networkId path parameter. Network ID
     */
    networkId?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a SwitchDscpToCosMappings resource.
 */
export interface SwitchDscpToCosMappingsArgs {
    /**
     * An array of DSCP to CoS mappings. An empty array will reset the mappings to default.
     */
    mappings?: pulumi.Input<pulumi.Input<inputs.networks.SwitchDscpToCosMappingsMapping>[]>;
    /**
     * networkId path parameter. Network ID
     */
    networkId: pulumi.Input<string>;
}
